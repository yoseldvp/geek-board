import React, { Component } from 'react';
import {
  Button,
  StyleSheet,
  Text,
  TextInput,
  View,
} from 'react-native';

import { Card, SimpleInput, Subtitle, Title } from '../../components/common';
import { colors } from '../../styles';



const styles = StyleSheet.create({
  container: {
    padding: 12,
  },
});